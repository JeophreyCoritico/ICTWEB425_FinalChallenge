@model IEnumerable<CM_API.Models.C102575814Interest>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.InterestDesc)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.InterestDesc)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.InterestCode }) |
            @Html.ActionLink("Details", "Details", new { id=item.InterestCode }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.InterestCode })
        </td>
    </tr>
}

</table>
